/** @format */

// @ts-nocheck
import Telegraf from 'telegraf'
import WizardSceneModule from 'telegraf/scenes/wizard/index.js'
export const WizardScene = WizardSceneModule
export const {
  TelegrafContext,
  Extra,
  Markup,
  UpdateType,
  BaseScene,
  Scene,
  session,
  Stage,
  Context,
  Composer
} = Telegraf
export const telegraf = Telegraf
